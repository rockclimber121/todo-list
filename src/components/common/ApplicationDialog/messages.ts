import { defineMessages } from "react-intl";

const messages = defineMessages({
    okButtonCaption: {
        defaultMessage: "OK",
        description: "OK button caption in application dialog.",
        id: "components.ApplicationDialog.okButtonCaption",
    },
});

export default messages;
