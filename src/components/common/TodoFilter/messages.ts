import { defineMessages } from "react-intl";

const messages = defineMessages({
    onlyFavorite: {
        defaultMessage: "Only favorite",
        description: "Only favorite switch caption.",
        id: "components.TodoFilter.onlyFavorite",
    },
    onlyNotCompleted: {
        defaultMessage: "Only not completed",
        description: "Only not completed switch caption.",
        id: "components.TodoFilter.onlyNotCompleted",
    },
});

export default messages;
